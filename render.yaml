services:
  - type: web
    name: graphpassword
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn GraphPassword.wsgi
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.4